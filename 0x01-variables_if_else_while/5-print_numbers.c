#include <stdio.h>

/**
 * main - starting poibt of program
 *
 * Return: always 0 when true
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
